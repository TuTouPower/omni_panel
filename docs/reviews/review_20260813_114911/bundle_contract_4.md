# Review Bundle — contract | chunk 4

- Perspective: contract
- Chunk: 4（bundle 列表按 `id` 排序后取 `index % 6 == 3`）
- Reviewed bundles: 18（connectors_cpa, connectors_grok, connectors_tikhub, scripts_repo_template, src_main_core_connector, src_main_core_open_connectors_dir_ts, src_main_core_settings_close_action_ts, src_main_ipc, src_preload_route_api_ts, src_renderer_components_AddAccountDialog_tsx, src_renderer_components_CpaAddDialog_tsx, src_renderer_components_Icon_tsx, src_renderer_components_ProviderOverview_tsx, src_renderer_components_TrendSparkline_tsx, src_renderer_components_WebLoginSection_tsx, src_renderer_components_session_shell, src_renderer_index_tsx, src_shared_constants_ts）
- Reviewed files: 50
- HEAD SHA: `51ea3972efefea568cc2fba3e530ea5f69296182`

## Findings

- [High][90] src/preload/index.ts:201 — sessionHistory.open 用 IpcResult 包装非 IpcResult 通道，每次调用必抛 "Invalid IPC response" — main/index.ts:522-528 注册的 SESSION_HISTORY_OPEN handler 无 return（箭头函数块体，`open_or_focus(loc)` 后隐式返回 undefined），而 preload `session_history_full_methods.open` / `session_history_open_only_methods.open`（index.ts:201/307）走 `invoke<undefined>()`，其 `is_ipc_result(undefined)` 判定 `typeof undefined !== "object"` 失败直接 throw。所有调用点（TrayMenu.tsx:79、PopupView.tsx:711、TokenStatsView.tsx:1060/1070、panel-navigation.ts:19、SessionLibrary.tsx:353/553）都是 `void ...open(...)` → 每次点击会话入口产生 unhandled promise rejection（桌面渲染器 console 报错）；窗口打开靠 handler 副作用生效，功能勉强可用但 API 契约（UsageboardApi.sessionHistory.open 承诺 IpcResult 包裹）确定破坏。修复：main handler 返回 `ok(undefined)`，或 preload 该通道改用裸 `ipcRenderer.invoke`（不校验响应信封），并在 `is_ipc_result` 上留测试覆盖。

- [Medium][95] src/renderer/components/AddAccountDialog.tsx:182 — 保存失败完全静默：try/finally 无 catch，error_message 永不被赋值 — handle_save 的 apikey/session 路径（params 构造 → `on_save(params)`）失败时（如 CONFIG_CREATE_INSTANCE 返回 VALIDATION_ERROR、CONFIG_SAVE 并发 CONFLICT「配置已被其他窗口修改」、secret 保存失败），`void handle_save()`（:328）产生 unhandled rejection，对话框停留在 auth 步骤且无任何提示（:305-311 的 error_message/role=alert 块是死 UI）。修复：catch 后 `set_error_message(err.message)`，并处理 `saving` 状态复位。对比：OAuthDeviceForm/WebLoginForm/ExaServiceKeyForm/CpaMgmtForm 均有 catch 显示错误，仅此通用路径缺失。

- [Medium][85] connectors/grok/connector.ts:119 — get_legacy_reset_at 的 Date.parse 未校验 NaN，reset_at 违反 observation schema 导致整条观测被 runtime 丢弃 — `typeof config.billingPeriodEnd === "string" ? Date.parse(...) : null`；billingPeriodEnd 为不可解析字符串时返回 NaN，经 get_legacy_period（:145）与 legacy_reset_at（:221）写入观测 `reset_at`，而 script_observation_schema 的 `reset_at: finite_number.nullable()`（src/shared/schemas/observation.ts:42）拒绝 NaN → runtime.ts:184-187 跳过该观测；credits 与全部 productUsage 观测连带被丢 → observations.length===0 → 误报 "billing response has no usable usage fields"（:234-241）。可复现：API 返回 `{"billingPeriodEnd": "2026-08-13T00:00:00Z"}` 之外的任意非 RFC3339/非 Date.parse 可解字符串。修复：`const ts = Date.parse(...); return Number.isFinite(ts) ? ts : null;`（与 parse_rfc3339 的严格路径对齐）。

- [Medium][70] connectors/grok/connector.ts:200 — productUsage 无校验 cast，product.product 缺失时 TypeError 中断整轮刷新 — `config.productUsage as readonly ProductUsage[]` 后直接 `to_snake_case(product.product)`（:208）；proto3 会省略缺失标量字段，若某 product 缺 `product` 字段 → `undefined.replace` TypeError → main() reject（无 try/catch）→ run_connector 整体失败，refresh-service 重试 3 次后 failed，其余合法产品观测全部丢失。修复：循环内先 `typeof product.product === "string"` 校验（与 usagePercent 的校验同级），或对单条解析 try/catch 跳过。

- [Low][100] src/renderer/components/Icon.tsx:71 — 三处调用使用未注册图标名 "alert_circle"，错误提示图标静默渲染为空 — UI_ICONS（Icon.tsx:71-121）无 alert_circle（git log -S 证实从未存在），WebLoginSection.tsx:107、SessionSection.tsx:53、DeviceLoginSection.tsx:214 均 `<Icon name="alert_circle" />`；Icon 对未知 name 返回空 SVG（:175-191），错误行只剩文字+空白占位。修复：注册 lucide 对应图标（CircleAlert）或改用已注册的 info/close，并考虑对未知 name 打 warn。

- [Low][90] src/preload/index.ts:355 — config.export 的 includeSecrets 选项在桌面端被丢弃，与类型契约不符 — UsageboardApi.config.export 声明 `(options?: ConfigExportOptions) => Promise<{saved:boolean}>`（shared/types/ipc.ts:560，含 includeSecrets），但 preload `config_full.export` 内 `void options` 且 CONFIG_EXPORT 通道不传参（config-ipc.ts:712-715 的 handler 无入参）；桌面 dialog 导出恒调用 handleConfigExport 内嵌全部 secrets（config-ipc.ts:555）。includeSecrets 仅 /v1/config/export 本地 API/CLI 路径生效（local-api/server.ts:1347、cli/client.ts:155）。当前桌面调用方均不传 options（SettingsView.tsx:264 仅 web_mode 传），属潜伏错配。修复：桌面实现该选项，或在类型与注释中明确「桌面导出恒含 secrets、options 仅 web/CLI 生效」。

- [Low][80] src/main/ipc/trend-ipc.ts:43 — TREND_GET_BULK 无入参校验，畸形 payload 直接 TypeError reject — 同族 token-stats-ipc 全用 zod schema 校验（tokenStatsDashboardQuerySchema 等），此处 `payload.periods.map(...)`（:46）裸访问；payload 缺 periods / period 非对象 / metric_id 缺失 → TypeError → ipcMain.handle reject → preload invoke 抛 `[UNKNOWN] ...`。修复：为 TrendBulkRequest 加 zod schema，失败返回 fail("VALIDATION_ERROR")。

- [Low][75] scripts/repo_template/track_worktree.py:35 — TERMINAL_EVENTS 含 ledger 从未写入的事件名，drop 后 worktree 状态行可能残留 — ledger 实际事件仅 start / attempt_reserved / attempt_terminal / report / integrated（repo_task/attempts.py:237/259/299/371、integration.py:248），而 TERMINAL_EVENTS 声明 {"report","integrated","drop","attempt_terminal","attempt_report"} 中的 "drop"/"attempt_report" 无对应写入路径；task drop 后 resolve_worktree 不会因 drop 事件关闭 active 窗口，状态行仍指向已删 worktree。修复：移除无效事件名或让 drop 生命周期写入 ledger。

- [Low][100] src/renderer/views/SettingsView.tsx:123 — CpaAddDialog 永不渲染且保存按钮无行为，死 UI — setShowCpaAdd 从未以 true 赋值（仅 :688 false 分支），CpaAddDialog 实际不可达；即使可达，其「保存并同步」（CpaAddDialog.tsx:66-68）与「测试连接」（:59-62）均无 onClick，不触发任何保存逻辑。修复：删除该组件与 showCpaAdd 状态，或在启用入口时补齐 on_save 接线（CPA 添加已由 AddAccountDialog 的 CpaMgmtForm 路径覆盖，此组件是遗留双入口）。

- [Info][85] scripts/repo_template/render_review_prompts.py:29 — 模板未知占位符静默透传 — PLACEHOLDER_RE 只替换白名单内 9 个占位符，模板中出现 `{typo}` 时不报错、原样输出到 prompt；code/test/general/share 四个模板与 values 键的一致性无验证。修复：渲染后检测残留 `{...}` 并报错。

- [Low][40] connectors/cpa/connector.ts:356 — parse_kimi 空 period_label 产出空 raw_label，观测被 schema 丢弃 — kimi limits 条目 name/title/duration/timeUnit 全缺时 period_label 为 "" → raw_label: "" 违反 `raw_label: z.string().min(1)`（observation.ts:27）→ runtime 丢弃；metric_id 尾部空段（`kimi:{account}:`）也弱化匹配。修复：空标签回退 "unlabeled"，metric_id 用 entry index 兜底。

## Reviewed files（50，全部当前 HEAD 内容）

connectors/cpa/connector.ts, connectors/cpa/manifest.json, connectors/grok/connector.ts, connectors/grok/manifest.json, connectors/tikhub/connector.ts, connectors/tikhub/manifest.json, scripts/repo_template/\_id_scan.py, scripts/repo_template/check_review_status.py, scripts/repo_template/findings.py, scripts/repo_template/pending.py, scripts/repo_template/render_review_prompts.py, scripts/repo_template/repo_state.py, scripts/repo_template/spikes.py, scripts/repo_template/task.py, scripts/repo_template/track_worktree.py, src/main/core/connector/host-io.ts, src/main/core/connector/manifest-loader.ts, src/main/core/connector/net-client.ts, src/main/core/connector/probe-executor.ts, src/main/core/connector/runtime.ts, src/main/core/connector/script-cache.ts, src/main/core/connector/tier1-poll-executor.ts, src/main/core/open-connectors-dir.ts, src/main/core/settings-close-action.ts, src/main/ipc/auth-ipc.ts, src/main/ipc/build-info-ipc.ts, src/main/ipc/config-ipc.ts, src/main/ipc/connector-ipc.ts, src/main/ipc/event-ipc.ts, src/main/ipc/grok_auth_ipc.ts, src/main/ipc/helpers.ts, src/main/ipc/kimi_auth_ipc.ts, src/main/ipc/log-ipc.ts, src/main/ipc/logged.ts, src/main/ipc/popup-ipc.ts, src/main/ipc/session-history-ipc.ts, src/main/ipc/session-ipc.ts, src/main/ipc/size-validation.ts, src/main/ipc/token-stats-ipc.ts, src/main/ipc/trend-ipc.ts, src/preload/route_api.ts, src/renderer/components/AddAccountDialog.tsx, src/renderer/components/CpaAddDialog.tsx, src/renderer/components/Icon.tsx, src/renderer/components/ProviderOverview.tsx, src/renderer/components/TrendSparkline.tsx, src/renderer/components/WebLoginSection.tsx, src/renderer/components/session-shell/SessionShell.tsx, src/renderer/index.tsx, src/shared/constants.ts

## 执行过的只读检查

- 契约对照：IPC 通道注册（src/main/ipc/\*、main/index.ts）vs preload 包装（src/preload/index.ts、route_api.ts、oauth_api）vs shared/types/ipc.ts 类型面；逐一核对通道名、参数序、响应信封（IpcResult / 裸值 / 推送事件）。
- Manifest/schema 对照：connectors/\*/manifest.json vs shared/schemas/manifest.ts、auth.ts（auth.method "cpa_mgmt" 合法、secret_name 与参数一致）、plugin-output.ts（usageProviderSchema 含 cpa monitor 四 provider）、plugin-metadata.ts（number→integer 映射、secret default 不泄漏）。
- 连接器运行时链路：net-client.ts（origin 守卫对 cpa 绝对 URL 的判定——cpa 的上游绝对 URL 在 payload 而非 path，不受限）、refresh-service.ts build_params/secret 注入契约、runtime.ts script_observation_schema 校验行为（grok NaN/空 raw_label 丢弃路径）。
- 渲染器数据流：AddAccountDialog → AccountDialog → SettingsView onAddAccount → use_connector_catalog.create_instance_and_save → CONFIG_CREATE_INSTANCE/CONFIG_SAVE_SECRETS；auth-flow-registry fallback_secret_name 与 build_params vault 键一致性；secretParamKeys 经 onConfigSaved 重建（main/index.ts:536-539）确认新建实例可存 secret。
- 会话历史：session-history-ipc key_of 与 subscription-service loc_key 格式一致；SESSION_HISTORY_OPEN handler 返回值与 preload invoke 信封不匹配（已确认 bug）；WebLoginSection AUTO_CLOSE_MS 1500 与 auth-ipc.ts:14 一致；session-manager 支持 "\*" cookie 通配。
- git log / git blame：net-client origin 守卫（t294 3d770c70）、CpaAddDialog（74f23d69/755b0b01/172b880c）、Icon alert_circle（git log -S 无记录）、t337/t331 提交记录。
- 脚本工具链：task.py façade 与 repo_task/\* 导出面、pending/findings/spikes 共用 \_id_scan 锁语义、check_review_status/render_review_prompts 指纹口径一致性、repo_state.py 只读性质。

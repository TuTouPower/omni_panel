# p128 ui Switch 暗色下圆钮与关态轨道对比度待裁决

- 来源：t301 遗留（code review f002）
- 内容：Switch 关态轨道对齐 `switch-track` 后改用 `surface-raised`；暗色下圆钮 `surface-window`（#181b22）对轨道 `surface-raised-dark`（#262b34）对比约 1.23:1 近不可辨。DESIGN 正文称开关为「白色圆钮」，与当前 `surface-window` 圆钮色不符。圆钮颜色超出 t301 六项范围，需设计侧裁决：圆钮改白色（on-surface/白）或维持语义色。
- 处理：t301

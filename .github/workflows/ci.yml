name: CI

on:
    pull_request:
        branches: [master, main]
    push:
        branches: [master, main]

concurrency:
    group: ci-${{ github.ref }}
    cancel-in-progress: true

jobs:
    check:
        name: Quality checks
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: pnpm/action-setup@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: pnpm
            - run: pnpm install --frozen-lockfile
            - run: pnpm check
            - name: Check schema freshness
              # 导出产物须与 zod 源一致；导出后 prettier 与提交格式对齐，漂移即失败。
              run: pnpm schema:export && pnpm prettier --write schemas/ && git diff --exit-code schemas/
            - name: Install gitleaks
              uses: gitleaks/gitleaks-action@v2
              with:
                  args: detect --source .
              env:
                  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    test:
        name: Unit & integration tests
        strategy:
            fail-fast: false
            matrix:
                # d062：macOS 平台假设只能在 macOS 上暴露（历史 p227/p236/p238 均属此类）。
                os: [ubuntu-latest, windows-2022, macos-latest]
        runs-on: ${{ matrix.os }}
        steps:
            - uses: actions/checkout@v4
            - uses: pnpm/action-setup@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: pnpm
            - run: pnpm install --frozen-lockfile
            - run: pnpm test

    e2e:
        name: E2E tests
        runs-on: windows-2022
        steps:
            - uses: actions/checkout@v4
            - uses: pnpm/action-setup@v4
            - uses: actions/setup-node@v4
              with:
                  node-version: 22
                  cache: pnpm
            - run: pnpm install --frozen-lockfile
            - name: Package app
              run: pnpm package
              env:
                  ELECTRON_MIRROR: "https://npmmirror.com/mirrors/electron/"
            - name: Install Playwright browsers
              run: npx playwright install --with-deps chromium
            - name: Run web e2e smoke (synthetic fixture)
              # synthetic seed fixture 入库（脱敏假账号），CI 可跑 web SPA 数据链路
              env:
                  MOCK_FIXTURE: synthetic
              run: pnpm test:e2e:web
            - name: Run packaged smoke tests
              # packaged smoke 验证 exe 产物；electron 驱动 nightly 跑
              run: pnpm test:packaged
